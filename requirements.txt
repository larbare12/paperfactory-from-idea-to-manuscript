# paper.skill v0.5+ Python dependencies
# Required only for `paper_search.sh --mode multi` (multi-source + BM25).
# Install: pip install -r requirements.txt
rank-bm25>=0.2.2
arxiv>=2.1.0
requests>=2.28.0
